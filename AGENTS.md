# 工程协作规则

本文件是仓库内工程规则的统一入口；用户的明确任务授权仍优先于仓库文档。更新规则时同步相关决策和实现证据，不能用文档声明替代验证。

## 工作入口

先读 [docs/README.md](docs/README.md)、[路线图](tasks/plan.md)与[任务清单](tasks/todo.md)。规范要求、基线代码行为和验收结果分开阅读；`docs/archive/` 是历史资料，不是当前约束或运行指导。

## 架构和设备职责

- 上位机发送经过认证、权限和状态校验的请求；STM32 负责实时流程、MFC 配气、仪表控制、传感器采集和设备安全处置，硬接线联锁拥有最终切断权。
- 不增加绕过联锁、强制打开 CO、强制恢复加热许可、直接写物理 I/O 或越过板端任务控制仪表的操作入口。
- 上位机断线时停止放行新控制请求并重连；板端按其已经验证的独立安全策略运行。不得根据网络恢复自动恢复试验。
- 启停等 CO 相关请求须二次确认、记录操作者并保留审计。参数修改统一经校验、下发、回读、归档；未知或过期设备状态不得作为控制许可。
- 试验原始采样和事件保持追加。报警确认等业务元数据变化保留审计；不能以“只追加”为由忽略实现中的确认状态更新。修正历史数据使用可追溯补录，不覆盖原值。
- 保留 Vue/FastAPI/SQLAlchemy/SQLite；公共契约或重要选型变更先记录 ADR，再随代码验证交付。当前目标环境见路线图，不使用“技术栈永不可变”的约束。

## 实现和验证

- Python 使用类型标注、异步 I/O、Black/isort（120 列）；Vue 使用 Composition API，网络调用集中在 `frontend/src/api/`。具体工具配置以仓库配置为准。
- 后台是权限和控制判断的执行方，前端门控只用于交互。公共输入验证、错误码、超时和失败语义须有行为测试。
- 先复现缺陷，再最小修改并验证；不以镜像实现的测试代替边界、故障和恢复测试。纯文档修正运行文档检查，无需无关全量业务测试。
- Alembic 已发布迁移只追加；迁移前备份并验证恢复。数据库、证书、密钥、一次性口令和实验导出不能提交。
- 国标数值标出条款和温度/单位对象；不凭 OCR 猜测公式。原文冲突登记为待决，并保存采用规则的版本和确认依据。
- 测试结论绑定提交、环境和产物。未做 Windows/真机测试必须标注“未验收”，不要在 README 维护无来源的测试数量。

## Git 与版本

长期分支为 `main`，工作从当前主线建立短期分支。必要 CI 通过后经 PR squash 合入，合并后删除工作分支。以 Conventional Commits 描述最终改动。

本次整合是一次性例外：`dev@855c84d` 的历史经保留祖先关系的整合 PR 纳入 `main`，UI 独有改动另行审查、squash。清理前验证 Git bundle 可恢复；不重写主线或移动既有标签。[ADR-003](docs/decisions/ADR-003-project-baseline.md)记录理由。

`backend/app/__init__.py` 的 `__version__` 是应用版本真源；前端、包清单和标签必须一致。发布流程见[发布与维护指南](docs/发布与维护指南.md)。提交、推送和 PR 在用户授权范围内完成，不因本文件额外要求重复确认。
