# ===== smd-web-hmi 后端运行依赖（修改后须重新生成 requirements.lock） =====
# Web 框架
fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.32.1
python-multipart==0.0.32

# 数据库（SQLAlchemy 2.x async + SQLite）
SQLAlchemy[asyncio]==2.0.36
aiosqlite==0.20.0
alembic==1.14.0

# 配置 / 校验
pydantic==2.10.3
pydantic-settings==2.6.1

# 认证（JWT）
PyJWT==2.13.0

# 结构化日志
structlog==24.4.0

# 商用报告导出（PDF / XLSX）
reportlab==5.0.1
openpyxl==3.1.5
