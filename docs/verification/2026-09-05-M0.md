# M0 文档基线验证记录

- 要求：M0-01、M0-02、M0-03、M0-04。
- 文档/检查器实现提交：`91c1623240d82cefea3ab81a4e5b3246715d6919`。
- 所核查业务基线：`855c84d425f35279cabb9ac9ed6dc322711c335c`，应用`0.3.0-rc.1`。
- 环境：Darwin arm64，Python 3.9.6，仅Python标准库；本项未安装或变更业务依赖。
- 级别：software（文档静态核验）；设备/固件：not_applicable。
- 维护角色：项目文档实现者；设备和试验签字不属于本项。

## 步骤与结果

1. 运行 `python3 scripts/check_docs.py`：通过；33份Markdown、101个本地链接，包含中文文件名、目录和历史档案。
2. 暂存一份包含不存在文件和不存在中文章节锚点的临时Markdown，运行同一检查器：以退出码1准确报告两项错误；删除临时文件后再次通过。
3. 运行 `git diff --check`：通过；首个提交后工作树干净。
4. 目视核对标准PDF第12、13、17页：装样两处焦炭量、H600公式、温度条件、温差编号和附录B表/边界对应当前实验规范；来源PDF hash记录于实验规范。
5. 对照基线代码确认启动请求H必填、WS首帧认证与不支持subscribe、状态字段差异、参数通用入口绕过回读、停止受理即收尾及现有认证撤销机制。

## 结论与限制

M0文档结构、要求映射、外部依赖、历史追溯、ADR和路线图已完成。该证据不声明M1—M5实现、CI运行、Windows安装或真实STM32验收通过。后续任务需另行绑定实现SHA及软件/模拟器/Windows/真机证据。

本记录及任务状态更新会增加Markdown/链接数量，后续检查应报告实际计数；不得把上面的历史计数作为固定通过条件。
