# Independent test tool only; never installed into the application freeze environment.
# Constraints keep shared dependencies compatible with the deployed backend.
-c ../../backend/requirements.lock
-c ../../backend/requirements-dev.lock
-c ../../desktop/requirements.lock
playwright==1.62.0
pypdf==6.17.0
httpx==0.28.1
pydantic==2.10.3
openpyxl==3.1.5
pywin32==312; sys_platform == 'win32'
